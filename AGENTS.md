# EasyContentCreator Agent 协作规范

## 开始任务前

依次阅读 `AGENTS.md`、`docs/vision.md`、`docs/architecture.md`、当前任务文件，以及任务引用的 ADR 和交接记录。仓库文档是项目上下文的权威来源，聊天记录不能替代文档。

## 任务边界

- 一次只执行一个 `ECC-NNN` 任务，并遵守任务声明的文件范围。
- 分支使用 `task/ECC-NNN-short-slug`；并行任务必须使用独立 Git Worktree。
- 发现公共接口、架构或安全决策缺失时先停止实现，补充或提议 ADR。
- 不顺带实现未列入验收条件的能力，不引入微服务、独立消息队列、多租户或兼容双路径。
- 不修改其他 Agent 的工作树；跨任务协作通过任务、ADR、PR 和 handoff 完成。

## 实现与验证

- 实现、测试和相关文档必须在同一任务中更新。
- 运行任务列出的验收命令；失败不得标记完成。
- 提交前检查 `git status --short`、`git diff --check` 和实际 diff 范围。
- 任何密钥不得写入仓库、日志、样例或测试固定数据。

## 完成交接

- 更新任务文件中的结果、测试证据、偏差和后续事项。
- 创建 `docs/handoffs/ECC-NNN.md`。
- 任务完成后移动到 `docs/tasks/completed/`；阻塞则移动到 `docs/tasks/blocked/` 并写明解除条件。
- 提交信息和 PR 标题必须包含任务编号，合并采用 Squash。

## 模型分级

- `low`：脚手架、简单 CRUD、固定映射、测试补齐、文档同步和机械修改。
- `medium`：单模块逻辑、Web 页面、Worker 步骤、FFmpeg 封装和普通故障修复。
- `high`：架构、状态机、迁移、并发幂等、安全、跨模块集成与复杂诊断。

低价模型只有在接口冻结、文件范围明确且验收命令可执行时使用；连续两次验收失败或出现设计歧义时升级模型。
