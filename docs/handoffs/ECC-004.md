# ECC-004 交接记录

- 任务：`ECC-004`
- 分支：`task/ECC-004-scaffold`
- 提交：本交接所在提交
- 状态：已完成

## 已完成

- FastAPI 存活/就绪探针、环境配置与 PostgreSQL 连接边界。
- 可优雅停止的数据库轮询 Worker 生命周期骨架。
- React + Vite + TypeScript 健康页及 Python/Web 基础测试。
- 本地开发 runbook 与无密钥 `.env.example`。

## 验证证据

- `/private/tmp/ecc-scaffold-venv/bin/ruff check .`：通过。
- `/private/tmp/ecc-scaffold-venv/bin/mypy`：通过，16 个源文件无问题。
- `/private/tmp/ecc-scaffold-venv/bin/pytest`：通过，4 项测试通过；依赖层有一条 Starlette/httpx 弃用提示。
- `npm run lint && npm test && npm run build`：通过，1 项 Web 测试通过并成功生成生产构建。
- `python3 -m compileall -q apps packages tests` 与 `git diff --check`：通过。

## 决策与偏差

- 存活探针不访问数据库；就绪探针失败关闭并返回 503。
- Worker 不查询尚未定义的任务表，避免提前固化 M1 设计。
- 本 worktree 尚无 ECC-003 架构产物；仅实现父任务明确要求的基础设施。

## 后续执行者须知

M1 应通过迁移和冻结的认领接口扩展 `PollingWorker.poll_once`，不要在健康检查中承载业务诊断。
