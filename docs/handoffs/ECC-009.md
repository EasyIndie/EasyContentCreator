# ECC-009 交接记录

- 任务：`ECC-009`
- 父任务：`ECC-008`
- 分支：`task/ECC-009-api-version`
- 提交：本任务提交（以 Git 历史为准）
- 状态：completed

## 已完成

- 配置增加构建版本与提交标识，支持 `ECC_VERSION`/`ECC_COMMIT` 注入及安全缺省值。
- API 增加 `GET /version`，响应严格为 `version` 和 `commit` 两个字符串字段。
- 增加缺省配置与构建环境覆盖测试。

## 验证证据

- `ruff check apps/common apps/api tests`：通过。
- `mypy apps packages`：通过。
- `python -m pytest`：6 项通过。共享虚拟环境为主工作树 editable 安装，独立 worktree 必须使用模块入口以确保导入当前检出。
- `git diff --check`：通过。

## 决策与偏差

复用现有 `Settings` 和 FastAPI 依赖覆盖机制，没有新增配置层或修改架构；与任务计划无偏差。

## 后续执行者须知

Web 子任务可直接请求 `/version`。部署环境需同时设置 `ECC_VERSION` 和 `ECC_COMMIT`，否则接口会明确返回开发缺省值。
