# ECC-012 交接记录

- 任务：`ECC-012`
- 分支：`main`
- 提交：`ea81ad5`
- 状态：completed

## 已完成

Deploy Staging 与 Release 使用 Bash 小写转换生成统一 GHCR 前缀，构建标签和 digest 引用共享同一输出。

## 验证证据

- 工作流 YAML 可解析。
- staging `31296192436` 成功推送 API/Web digest 镜像；SSH 步骤确认缺少服务器配置。

## 决策与偏差

转换在独立步骤完成，避免在多个表达式中重复拼接并依赖 GitHub 表达式不具备的大小写函数。

## 后续执行者须知

新增镜像时复用 `steps.registry.outputs.prefix`。
