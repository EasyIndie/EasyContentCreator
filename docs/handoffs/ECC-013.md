# ECC-013 交接记录

- 任务：`ECC-013`
- 分支：`main`
- 提交：待提交
- 状态：completed

## 已完成

staging 部署增加仓库变量门禁，并在部署运行手册记录启用所需 Secrets。

## 验证证据

- 工作流 YAML 解析通过。
- 文档检查通过。

## 决策与偏差

采用显式启用变量，不将缺少服务器视为每次代码迭代的 CI 故障。

## 后续执行者须知

只有服务器、部署目录、SSH 密钥和备份目录全部就绪后才设置 `STAGING_ENABLED=true`。
