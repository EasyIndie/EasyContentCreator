# ECC-017 交接记录

- 任务：`ECC-017`
- 分支：`task/ECC-017-persistence`
- 提交：本交接所在提交
- 状态：completed

## 已完成

- `001_initial.sql` 建立项目、来源与片段、不可变产物、上游血缘、来源引用、current pointers、jobs 和 reviews。
- Python runner 在单事务中按编号应用迁移、锁定迁移表并保证重复执行无副作用。
- Project Repository 提供乐观并发更新；Source/Artifact Repository 提供不可变写入与完整读取。
- PostgreSQL 外键保证 current pointer 和上游属于同一项目，引用指向真实来源片段；触发器禁止历史来源、产物和血缘被更新或删除。
- 测试 fixture 为每次 session 建立随机 `ecc_test_*` schema，绝不清空既有 public/shared schema。

## 验证证据

- 数据库：本地 Docker `postgres:16-alpine`，独立数据库 `ecc_test`。
- 迁移：空 schema 首次应用 `001_initial.sql`，第二次返回零项。
- Repository：6 项真实 PostgreSQL 测试通过，包括嵌套 JSONB/证据血缘 round-trip、两连接并发 revision 冲突、重复写入、历史变更和无效指针失败。
- Ruff：`apps/common`、`packages/domain`、迁移 runner 和测试通过。
- Mypy：`apps/common`、`packages/domain` 和迁移 runner strict 模式通过。
- `./scripts/check-docs.sh` 与 `git diff --check`：通过。

## 决策与偏差

- 未引入 Alembic；迁移仍为纯 SQL 和最小 Python runner。
- Job schema 使用 ECC-019 冻结的 `queued/running/succeeded/failed`；Review schema使用 ECC-020 冻结的 decision/note/project_revision 字段。
- 未修改 `Database`，因为现有 context manager 已为每次 Repository 写入提供提交/回滚事务。

## 后续执行者须知

- 已应用迁移不可编辑；ECC-019/020 若需扩展 schema，必须新增 `002_*.sql`。
- `ProjectRepository.update` 要求传入对象 revision 严格等于 `expected_revision + 1`。
- current pointer 的 kind 必须与所引用 Artifact kind 一致；Artifact upstream 必须属于同一项目。
- 集成测试必须设置 `TEST_DATABASE_URL`；缺少真实 PostgreSQL 时会 skip，不能据此宣称任务验收通过。
- PostgreSQL 触发器落实 MVP 保留全部历史的约束；未来垃圾回收必须通过新 ADR 与追加迁移显式改变。
