# ECC-018 交接记录

- 任务：`ECC-018`
- 分支：`task/ECC-018-fake-adapters`
- 提交：本交接所在提交
- 状态：completed

## 已完成

- `FakeProvider` 使用规范化 JSON 派生 Artifact UUID、SHA-256、安全相对路径和 consumed units，并写入可复现 UTF-8 JSON。
- `FakePublisher` 提供渠道校验、幂等 publish、status 与固定时间 metrics；同键不同请求失败关闭。
- 两个 Fake 均支持构造参数注入一次性 `RetryableError` 或持续 `PermanentError`，默认不访问网络、系统时间、随机数或环境密钥。
- 测试覆盖跨实例字节一致、关键输入变化、幂等冲突、未知状态、渠道校验及两类失败注入。

## 验证证据

- `.venv/bin/ruff format --check packages/providers packages/publishers tests/test_fake_adapters.py`：通过。
- `.venv/bin/ruff check packages/providers packages/publishers tests/test_fake_adapters.py`：通过。
- `.venv/bin/mypy packages/providers packages/publishers`：通过，6 个源文件无问题。
- `.venv/bin/python -m pytest tests/test_fake_adapters.py tests/test_adapter_contracts.py`：通过，10 项测试通过。
- `./scripts/check-docs.sh` 与 `git diff --check`：通过。

## 决策与偏差

- UUID 和 external ID 从规范化请求摘要派生，不使用随机数；时间完全由构造参数注入。
- 同路径已有相同字节时复用而不覆盖，已有不同字节时抛 `AdapterContractError`。
- 严格实现已冻结协议，未修改领域契约或增加外部 SDK。

## 后续执行者须知

ECC-023 应显式传入固定 UTC 时间、临时 artifact 根目录与失败模式；Publisher 重试必须复用原 Publication 及其 idempotency_key。
