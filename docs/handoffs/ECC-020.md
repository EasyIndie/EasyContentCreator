# ECC-020 交接记录

- 任务：`ECC-020`
- 分支：`task/ECC-020-project-review-api`
- 提交：本交接所在提交
- 状态：completed

## 已完成

- 新增项目创建、降序列表、详情与人工批准/驳回 HTTP 接口，Repository 和 UTC clock 均可通过 FastAPI dependency 替换。
- 项目响应固定包含 current artifacts；404/409 使用冻结错误结构，输入错误保留 FastAPI 422。
- 新增不可变 Review/ReviewDecision 与纯插入 `ReviewRepository.add(review)`。
- `ProjectRepository.apply_review` 在单一 PostgreSQL 事务内校验 decision/目标状态、以 `review_required + revision` 条件更新状态并插入 Review；标题和 current pointers 不允许通过审核改变，API 不包含 SQL。
- 测试覆盖创建、列表、详情、批准、驳回、未找到、陈旧 revision、非法状态及非法 note，并以真实 PostgreSQL 验证批准/驳回成功、decision/status 错配拒绝、两连接并发仅一个提交及 Review 插入失败整体回滚。

## 验证证据

- `ruff format --check apps/api packages/domain tests/test_project_api.py`：10 个文件已格式化。
- `ruff check apps/api packages/domain tests/test_project_api.py`：通过。
- `mypy apps/api packages/domain`：strict 模式 9 个源文件通过。
- 真实 PostgreSQL 16：rebase 最新主线后完整 59 项 Pytest 全部通过，包括 14 项项目/审核 API 与事务/并发测试、既有 Repository、Fake 和证据规则回归。
- `./scripts/check-docs.sh` 与 `git diff --check`：通过。
- Pytest 仅报告一条第三方 Starlette/httpx 弃用提示。

## 决策与偏差

- 为满足原子审核边界，对 `ProjectRepository` 最小追加 `list()` 和 `apply_review()`；既有 create/get/update 行为未改变。
- `ReviewRepository.add()` 保持只插入；审核用例调用事务型 `apply_review()`，避免跨 Repository 连接造成部分提交。
- 未新增迁移，复用 ECC-017 已冻结的 reviews 表。

## 后续执行者须知

- Web 可依赖项目响应中的固定字段和审核后的 201 Review 响应；项目新状态通过详情接口读取。
- 后续审核扩展不得拆开 `apply_review()` 的事务边界，也不得在 API 层直接访问数据库。
