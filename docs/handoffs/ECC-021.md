# ECC-021 交接记录

- 任务：`ECC-021`
- 分支：`task/ECC-021-web-console`
- 提交：本交接所在提交
- 状态：completed

## 已完成

- 新增轻量 typed API client，对项目、Artifact、健康、版本、Review 及冻结错误结构执行运行时校验。
- 单页控制台支持项目 loading/empty/error/content、创建、列表选择、详情和 current artifacts 展示。
- `review_required` 项目支持批准/驳回、必填说明、提交禁用防重复；409 自动刷新详情并显示固定冲突提示。
- 健康与版本请求独立降级为 unavailable，不遮蔽项目列表或详情。
- 表单 label、fieldset/legend、status/alert、键盘按钮和提交后焦点反馈满足基础可访问性边界。

## 验证证据

- `npm --prefix apps/web run lint`：通过。
- `npm --prefix apps/web test`：通过，9 项 Vitest 通过。
- `npm --prefix apps/web run build`：TypeScript 与 Vite 生产构建通过。
- `./scripts/check-docs.sh` 与 `git diff --check`：通过。

## 决策与偏差

- 未引入路由或全局状态库，使用一个 API client 与单页局部状态。
- 初始列表加载期间禁用创建，避免迟到列表响应覆盖新建项目。
- 网络错误、非 JSON 与未知响应结构统一映射为 unavailable；冻结 409 保留业务语义。
- 仅修改 `apps/web/**` 和本任务文档，无范围偏差。

## 后续执行者须知

ECC-023 可保持当前 HTTP 结构；若新增生成入口，应扩展 typed client 并保持项目数据请求与 health/version 独立失败。
