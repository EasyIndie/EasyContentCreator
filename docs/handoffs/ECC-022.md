# ECC-022 交接记录

- 任务：`ECC-022`
- 分支：`task/ECC-022-evidence-rules`
- 提交：本交接所在提交
- 状态：completed

## 已完成

- 新增纯函数 `validate_artifact_evidence(artifact, sources_by_id)` 与不可变 EvidenceReport/EvidenceIssue。
- FACT_CARD、SCRIPT 缺引用时失败关闭；所有 Artifact 已携带的 citation 均验证来源、片段与来源摘要快照。
- 固定支持 `citation_required`、`source_missing`、`excerpt_missing`、`source_digest_mismatch`、`duplicate_citation`。
- issue 按 code、source UUID、excerpt UUID 稳定排序；完全重复 citation 只报告一次 duplicate，不重复执行其余验证。

## 验证证据

- `.venv/bin/ruff format --check packages/pipeline tests/test_evidence_rules.py`：通过。
- `.venv/bin/ruff check packages/pipeline tests/test_evidence_rules.py`：通过。
- `.venv/bin/mypy packages/pipeline`：strict 模式通过，2 个源文件无问题。
- `.venv/bin/python -m pytest tests/test_evidence_rules.py tests/test_domain_contracts.py`：通过，23 项测试通过。
- `./scripts/check-docs.sh` 与 `git diff --check`：通过。

## 决策与偏差

- 规则仅消费调用者提供的内存映射，不访问 Repository、数据库、网络或时钟。
- 非事实产物可不带引用，但如果携带 citation 仍执行完整验证。
- 未修改领域模型、Repository、错误类型或已接受 ADR，无范围偏差。

## 后续执行者须知

ECC-023 必须先从 SourceRepository 装载所需 Source，再调用本规则；`report.valid` 为 false 时不得转换项目到 `review_required`。
