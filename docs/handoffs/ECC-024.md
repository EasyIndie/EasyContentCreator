# ECC-024 交接记录

- 任务：`ECC-024`
- 分支：`task/ECC-024-verify-parity`
- 提交：本交接所在提交
- 状态：completed

## 已完成

- 本地统一验证在 Ruff 可用时先执行 `ruff format --check .`，再执行现有 `ruff check .`。
- PR Fast Python job 将格式、Lint、类型与测试拆为独立步骤，保留 `python` job 和工作流名称。
- 完整 CI 继续调用同一 `scripts/verify.sh`，因此与本地使用完全相同的格式与 Lint 路径集合。
- 迭代手册补充格式修复命令、提交前入口和本地缺失可选工具的语义。

## 验证证据

- 人为加入未格式化的临时 Python 文件后，统一 `scripts/verify.sh` 在 `ruff format --check .`
  处按预期失败；移除探针后恢复通过。PR Fast 使用相同根目录命令，因此失败判定一致。
- `bash -n scripts/verify.sh` 与 `shellcheck scripts/verify.sh`：通过。
- `actionlint .github/workflows/pr-fast.yml .github/workflows/ci.yml`：通过。
- `./scripts/check-docs.sh` 与 `git diff --check`：通过。
- `POSTGRES_PASSWORD=validation-only ./scripts/verify.sh`：通过；Ruff format 检查 86 个文件、
  Ruff lint、Mypy 20 个源文件、18 项 Pytest、Web ESLint、4 项 Vitest、Web 生产构建及
  Docker Compose 配置检查全部通过。Pytest 仅报告一条第三方 Starlette/httpx 弃用提示。

## 决策与偏差

- 未修改检查名称、并发取消、缓存或路径过滤。
- 未修改应用行为；无需格式化现有 Python 文件。

## 后续执行者须知

后续新增 Python 路径必须继续由仓库根目录的两条 Ruff 命令统一覆盖，不要在工作流中维护另一套路径列表。
