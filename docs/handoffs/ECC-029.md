# ECC-029 交接记录

- 任务：`ECC-029`
- 分支：`task/ECC-029-evidence-read-api`
- 提交：本交接所在提交
- 状态：completed

## 已完成

- `GET /sources/{source_id}` 返回 Source 摘要与按持久化 position 排列的 excerpts。
- `GET /projects/{project_id}/artifacts` 在先确认项目存在后，返回按 kind、Artifact ID、version
  稳定排序的版本摘要。
- `GET /artifacts/{artifact_id}/versions/{version}?project_id=...` 在 Repository 查询中校验项目
  ownership，返回 metadata、upstream refs 与 citation 摘要快照。
- 所有响应模型都排除 `storage_path`；实现不访问 Artifact 文件，测试使用不存在的路径仍可成功读取
  数据库证据。
- Source、Artifact 不存在或 ownership 不匹配均使用冻结的 `not_found` envelope；非法 UUID、
  非法 version 与缺失 query 参数由 FastAPI 返回 422。

## 验证证据

- 隔离 Docker `postgres:16-alpine` 与随机 `ecc_test_*` schema：目标测试 8 项通过。
- 测试覆盖 Source/excerpt/SHA、嵌套 metadata、upstream、citation source/excerpt/SHA snapshot
  的真实 PostgreSQL round-trip，以及列表和嵌套集合顺序。
- OpenAPI 测试固定 Artifact 详情响应模型和必填 `project_id`，并断言整个 OpenAPI 文档及实际
  JSON 响应均不存在 `storage_path`。
- Ruff format（131 files）、Ruff lint、strict Mypy（9 source files）通过。
- 真实 PostgreSQL 下全量 Pytest 118 项通过；唯一 warning 为既有 Starlette/httpx 弃用提示。
- Web lint、9 项 Vitest、生产 build、docs、Compose config 与 `git diff --check` 均通过。

## 决策与偏差

- 任务指定的 Artifact 路径没有项目上下文，但验收要求跨项目访问失败关闭，因此增加必填
  `project_id` query 参数，不改变指定 URL path。
- Repository 新增 `get_for_project` 作为最小 ownership 查询；既有 `get` 继续保持兼容并委托该方法。
- 未增加迁移、状态变更、下载能力、文件读取或 Web 代码。

## 后续执行者须知

- ECC-030 调用 Artifact 详情时必须传当前项目 ID；404 不应向用户区分不存在与 ownership 不匹配。
- `metadata` 是持久化 JSON 的只读序列化；不得根据其中字段拼接服务器路径或读取文件。
- 若未来需要 Artifact 下载，必须按独立任务设计受控根目录、MIME 白名单与授权边界。
