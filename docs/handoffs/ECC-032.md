# ECC-032 交接记录

- 任务：`ECC-032`
- 分支：`task/ECC-032-web-job-console`
- 提交：本交接所在提交
- 状态：completed

## 已完成

- typed API client 增加冻结 `JobResponse` 列表校验与既有 generate API 请求/响应校验。
- 项目详情独立加载 Job 时间线；Job API 失败只降级任务区域，不遮蔽项目、Artifact 或审核操作。
- 时间线展示 queued/running/succeeded/failed、attempt、更新时间与安全 `error_class`，不消费或展示
  payload、lease、secret 或异常正文。
- 只有后端 `recoverable=true` 的 Job 显示恢复表单；前端不自行推导可恢复性。
- 恢复表单提交新 Idempotency-Key、Source IDs、template version 与整数 budget；提交期间禁用按钮，
  成功后刷新项目和 Job 并展示返回 Job ID；字段不清空，因此相同 key/body 可安全重放。
- 409 与 422 使用明确固定反馈，不回显后端异常详情。

## 验证证据

- `npm --prefix apps/web run lint`：通过。
- `npm --prefix apps/web test -- --run`：16 项通过。
- `npm --prefix apps/web run build`：TypeScript 与 Vite 生产构建通过。
- 测试覆盖四种 Job 状态、稳定 API 顺序、空列表、非法响应、恢复成功与同请求重放、409/422、
  单次提交门禁，以及 payload/secret/异常正文不进入 DOM。
- 文档与 `git diff --check`：通过。

## 决策与偏差

- 保持 ECC-021 的单页局部状态结构，未引入路由或全局状态库。
- 时间线使用 ECC-031 已稳定排序的响应顺序，不在客户端重新推导业务顺序。
- Source IDs 仅做逗号/空白拆分；UUID、重复值、strict budget 等最终约束继续由后端 422 负责。
- 未修改后端、数据库、payload 可见性或浏览器自动重试策略。

## 后续执行者须知

- ECC-034 可从控制台验证 failed Job 的错误分类，并用新 key 显式恢复到 queued/generating。
- 后续若新增 Job 字段，必须先冻结后端安全响应；不要让 Web 解析 payload 或异常正文。
