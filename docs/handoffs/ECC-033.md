# ECC-033 交接记录

- 任务：`ECC-033`
- 分支：`task/ECC-033-compose-migrations`
- 基线：`main@36baec7`
- 提交：本交接所在提交
- 状态：completed

## 已完成

- Compose 新增一次性 `migrate` service，复用 API 构建的统一 Python app image 和 `python -m migrations` 入口。
- migrate 等 PostgreSQL healthy 后执行；API/Worker 只依赖其 `service_completed_successfully`，迁移失败时不启动。
- 新增真实隔离 Compose E2E：fresh volume 应用仓库全部 migration、重复执行无新增记录、API readiness、Worker/Web 运行、坏 migration 非零且 API/Worker 未运行、专属资源清理。
- `scripts/verify.sh` 统一解析 `.venv` 或 PATH Python toolchain；缺少 python/ruff/mypy/pytest 或 `.venv` 不完整时立即非零，不再静默跳过。
- 本地开发 runbook 已切换到 `python -m migrations` 和自动 Compose 迁移流程。

## 验证证据

- `.venv/bin/ruff format --check apps migrations tests scripts`：30 个文件通过。
- `.venv/bin/ruff check apps migrations tests scripts`：通过。
- `.venv/bin/mypy apps migrations`：strict 模式 12 个源文件通过。
- `.venv/bin/python -m pytest tests/e2e/test_compose_migrations.py`：真实隔离 Compose 通过。
- `TEST_DATABASE_URL=postgresql://ecc:...@127.0.0.1:55433/ecc_test POSTGRES_PASSWORD=validation-only ./scripts/verify.sh`：Python 111/111、Web 9/9、生产构建、Compose 配置、docs/diff 全部通过；唯一 warning 为第三方 Starlette/httpx 弃用提示。
- `./scripts/test-verify-fail-closed.sh`：不完整虚拟环境正确返回非零。

## 决策与偏差

- migrate 不单独 build，和 Worker 一样复用 `ECC_IMAGE`；避免同标签多 build 覆盖入口。
- migration E2E 使用唯一 project、镜像标签和动态空闲端口；失败场景使用临时只读 `999_bad.sql` 挂载，不修改 001/002。
- 清理只针对测试生成的两个 project、卷和唯一 local images；最终额外 PostgreSQL 容器 `ecc033-full-db` 已删除。
- 验证脚本调整是 ECC-033 fail-closed 准出的核心范围；未修改 deploy、Actions、应用业务接口或历史 migration。

## 后续执行者须知

- 新增 migration 后无需修改 E2E 的文件名列表；测试会动态验证所有编号 SQL 均写入 `schema_migrations`。
- 不要让 API/Worker 自行并发运行 migration，也不要绕过 `service_completed_successfully`。
- CI 全量入口仍为 `scripts/verify.sh`；Python toolchain 缺失现在会显式失败。
