# ECC-033 交接记录

- 任务：`ECC-033`
- 分支：`task/ECC-033-compose-migrations`
- 基线：`main@574422f`
- 提交：本交接所在提交
- 状态：completed

## 已完成

- Compose 新增一次性 `migrate` service，复用 API 构建的统一 Python app image 和 `python -m migrations` 入口。
- migrate 等 PostgreSQL healthy 后执行；API/Worker 只依赖其 `service_completed_successfully`，迁移失败时不启动。
- 新增真实隔离 Compose E2E：fresh volume 应用仓库全部 migration、重复执行无新增记录、API readiness、Worker/Web 运行、坏 migration 与数据库不可达均非零且 API/Worker 未运行、专属资源清理。
- `scripts/verify.sh` 统一解析 `.venv` 或 PATH Python，并只用该解释器的 `python -m` 入口运行 Ruff、Mypy 与 Pytest；当前 worktree 通过 `PYTHONPATH` 和模块来源检查绑定，缺少工具或外部 editable 污染立即非零。
- 本地开发 runbook 已切换到 `python -m migrations` 和自动 Compose 迁移流程。

## 验证证据

- `.venv/bin/ruff format --check apps migrations tests scripts`：30 个文件通过。
- `.venv/bin/ruff check apps migrations tests scripts`：通过。
- `.venv/bin/mypy apps migrations`：strict 模式 12 个源文件通过。
- `.venv/bin/python -m pytest -m compose tests/e2e`：真实隔离 Compose 2 项通过。
- `TEST_DATABASE_URL=postgresql://ecc:...@127.0.0.1:55435/ecc_test POSTGRES_PASSWORD=validation-only ./scripts/verify.sh`：rebase 后 Python 快速集 129 项、Compose gate 2 项、Web 9 项、生产构建、Compose 配置、docs/diff 全部通过；唯一 warning 为第三方 Starlette/httpx 弃用提示。
- `./scripts/test-verify-fail-closed.sh`：不完整虚拟环境、外部 console script 与跨 worktree 模块导入回归通过。

## 决策与偏差

- migrate 不单独 build，和 Worker 一样复用 `ECC_IMAGE`；避免同标签多 build 覆盖入口。
- migration E2E 使用唯一 project、镜像标签和 Docker 随机发布端口；失败场景使用临时只读 `999_bad.sql` 挂载，不修改 001/002，并直接验证 migrate 非零退出与日志无密码。
- 清理逐个 best-effort 且只针对测试生成的三个 project、卷和唯一 local images；任务专用 PostgreSQL 容器在验收后删除。
- 验证脚本调整是 ECC-033 fail-closed 准出的核心范围；未修改 deploy、Actions、应用业务接口或历史 migration。

## 后续执行者须知

- 新增 migration 后无需修改 E2E 的文件名列表；测试会动态验证所有编号 SQL 均写入 `schema_migrations`。
- 不要让 API/Worker 自行并发运行 migration，也不要绕过 `service_completed_successfully`。
- CI 全量入口仍为 `scripts/verify.sh`；Python toolchain 缺失现在会显式失败。
- Compose E2E 标记为 `compose`；统一验证先跑快速 Python 集，再单独执行一次 Compose gate，避免同轮重复构建。
