# ECC-034 交接记录

- 任务：`ECC-034`
- 分支：`task/ECC-034-m1-exit-audit`
- 基线：`main@0013fc2`
- 提交：本交接所在提交
- 状态：completed

## 结论

M1 本地准出通过。真实 PostgreSQL、fresh Compose volumes、API/Worker、确定性 Fake、证据读取、
reject/新版本恢复、approve、Job 安全响应及 Web 生产构建均有可重复证据，无 M1 功能 blocker。

## 交付与证据

- 新增 `tests/e2e/test_m1_exit_smoke.py`，在独立 Compose project 中覆盖完整 v1 reject→v2 approve
  纵向切片、Source/Artifact evidence、Job 安全字段与宿主 Web 页面。
- 最终统一验证：Python 129 passed、Compose 3 passed、Web 22 passed、生产构建通过，72.30 秒。
- 独立 M1 Smoke：1 passed，28.27 秒。
- 准出矩阵、首次 Smoke 失败处置与风险记录在 `docs/iterations/M1-exit-audit.md`。
- 修正文档中已过时的“缺 Python 工具时静默跳过”描述，与 ECC-033 fail-closed 行为一致。

## 决策边界

- 未修改产品实现、migration、Actions、deploy、供应商或媒体能力。
- M0 远程 staging/回滚 blocker 与 M1 本地能力分开报告；本次不宣称生产就绪。
- M2 不复制 FACT_CARD 专用终态；先由 ECC-035 冻结通用步骤、媒体和发布包契约。

## 后续执行者须知

- 下一任务先创建 ECC-035 完整规格与相关 ADR，再实现 M2。
- `tests/e2e/test_m1_exit_smoke.py` 属于 `compose` marker，会由统一验证的 Compose gate 执行。
