# ECC-035 交接

## 结果

- ADR-007 冻结 `short_video_v1` DAG、logical artifact key、PipelineRun/StepRun/Job 终态、媒体文件提交和恢复协议。
- `short_video_9x16_v1`、QC、ReviewBundle 与 Douyin/WeChat Channels export-only 发布包边界已确定。
- M2 拆为 ECC-036～055；ECC-036 是唯一首个 Ready 公共接口任务。

## 关键约束

- 不得复制 FACT_CARD 专用 Generation terminal；首个步骤成功不得令项目进入 `review_required`。
- StepRun 钉住精确输入 refs；输出按 logical key 单调版本化，成功事务持有 live lease。
- 文件先同盘临时写、摘要、原子 rename，再提交 DB；允许孤儿，不允许 DB 指向半文件。
- 审核和发布包钉住 immutable bundle hash；无官方权限只导出，不做浏览器模拟、不标记 published。

## 验证

- `./scripts/check-docs.sh`
- `git diff --check`

## 后续

执行 ECC-036；完成后 ECC-037、ECC-039、ECC-040、ECC-041 可按依赖并行。M0 远程 staging/rollback 外部 blocker 不属于本任务。
