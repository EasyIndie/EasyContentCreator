# ECC-039 交接

## 结果

- `packages/providers/text.py` 提供版本化、规范 JSON 的 TOPIC_BRIEF/SCRIPT 确定性 Fake。
- `packages/pipeline/text.py` 实现 ECC-036 `StepProducer`：解析精确 binding/reservation，通过只读
  `InputArtifactResolver` 取得 pinned Artifact，并返回单一 `StepProductionResult`。
- TopicBrief 从 FACT_CARD 继承完整 citations；Script 从 TopicBrief 继续继承同一 closure。两步输出
  metadata 固定 input SHA、schema、template 和 provider version，upstream 固定精确输入 ref。

## 边界与失败语义

- Producer 不接收 Job/lease，不调用 `StepTerminalPort`，也不修改 StepRun、项目、数据库或 Worker。
- `TextEvidenceError`：输入无 citation、citation 重复/缺失/额外、resolver 返回非 pinned Artifact，或
  reservation 路径已存在不同字节。
- Provider bytes 必须是无重复字段的 canonical UTF-8 JSON；对象字段白名单、文档 type/schema、input SHA、
  citation keys、文本字段类型，以及 result 的 provider/schema/template version 均与冻结请求精确匹配。
- `TextBudgetExceeded`：Provider consumed units 或规范 UTF-8 JSON bytes 超出 `budget_units`。
- 无日志调用，异常仅包含分类语义，不包含输入或生成正文。
- 最终 JSON 使用同目录 `O_EXCL` 临时文件，完整写入并 fsync 后以 hard-link 无覆盖原子发布，再 fsync
  目录；相同 bytes 重试复用，已有异字节、symlink/非普通文件或崩溃残留临时文件失败关闭。

## 验证证据

- `ruff format --check .`：173 files formatted。
- `ruff check apps packages tests`：通过。
- `mypy apps packages migrations`：35 source files strict 通过。
- `pytest tests/test_text_steps.py` 增加恶意 Provider schema/citation/version 与安全原子落盘回归；最终验证
  23 passed。
- 全量非 Compose：104 passed、74 skipped、3 deselected；唯一 warning 为既有 Starlette/httpx 弃用提示。
- Compose gate：3 passed（99.75 秒）。首次沙箱内构建因 Buildx activity 目录不可写失败，沙箱外原命令
  真实通过，属于执行环境限制而非代码重跑规避。
- Web：ESLint、22 项 Vitest、TypeScript/Vite production build 通过；Compose config、文档门禁与
  `git diff --check` 通过。
- Web：ESLint 通过、22 tests passed、Vite production build 通过；当前 package 无 `typecheck` script，
  build 的 `tsc -b` 已通过。`check-docs.sh` 与 `git diff --check` 通过。

## 后续须知

- ECC-037/038 集成时应以 StepRun reservation 构造 `StepProductionRequest`，resolver 必须按项目 ownership
  返回精确 Artifact；终态 Repository 仍需再次验证 project/ref/citations 和落盘 SHA。
- 真实模型适配器必须复用相同 schema/closure/预算门禁，不得允许模型自行增删 citations。
