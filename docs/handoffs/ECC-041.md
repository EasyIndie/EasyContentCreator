# ECC-041 交接记录

- 任务：`ECC-041`
- 分支：`task/ECC-041-media-import`
- 基线：`main@a619916`
- 提交：本交接所在提交
- 状态：completed

## 已完成

- `packages/media.LocalAssetImporter` 只接受 import/artifact root 下规范 POSIX 相对路径；绝对路径、
  `..`、空段、反斜杠、NUL、源 symlink 和目标 symlink 链均失败关闭。
- JPEG/PNG/MP4/WAV/MP3 以内容签名和扩展名双重校验，调用方预期 SHA-256 必须与临时文件摘要一致。
- 导入在最终目录写 attempt 临时文件，flush/fsync 后在目标级协作锁内原子 rename；现有同摘要目标
  作为崩溃恢复复用，异摘要报不可变冲突，临时文件始终 best-effort 清理。
- `AssetSource`、`AssetRights`、`AssetManifestEntry` 与 `AssetManifest` 冻结来源、许可类型、证明 URI、
  commercial/modification/distribution scope、到期时间、Artifact ref、MIME 和字节数。
- `rights_issues(at=...)` 稳定返回 unknown、expired 与 scope missing code，供 ECC-043 映射为 QC blocker；
  public metadata 递归冻结且不包含文件路径。
- 错误消息只返回安全分类描述，不记录源名、目标路径、artifact root 或正文。

## 验证证据

- `python -m ruff format --check packages/domain packages/media tests/test_asset_import.py`：通过。
- `python -m ruff check packages/domain packages/media tests/test_asset_import.py`：通过。
- `python -m mypy packages/domain packages/media`：strict 通过。
- `python -m pytest tests/test_asset_import.py -q`：16 passed。
- `python -m pytest -m 'not compose' -q`：97 passed、74 skipped、3 deselected；唯一 warning 为既有
  Starlette/httpx 第三方弃用提示。
- `./scripts/check-docs.sh` 与 `git diff --check`：通过。

## 决策与边界

- 不改 ECC-036 公共冻结类型，仅新增独立素材权利契约并复用 `LogicalArtifactRef`。
- source/proof 只允许无凭据的 HTTPS URL 或 URN；服务器 `file:` URI 和存储路径不得进入 manifest。
- known license 必须有 proof URI；无到期时间表示长期有效，但仍须具备全部三项 M2 rights scope。
- 文件导入与数据库 StepRun 终态尚未耦合；ECC-038 handler 必须在文件完成后才提交 DB 终态。

## 后续执行者须知

- ECC-043 应直接消费稳定 `RightsIssueCode`，不要在 QC 内重新猜测 license 状态。
- 生产调用方必须由 reservation 生成 destination path，禁止把用户原始文件名直接作为 logical key 或
  artifact 路径。
- 后续增加 MIME 必须同时增加签名探测、扩展白名单和伪装回归，不能只依赖 `mimetypes`。
