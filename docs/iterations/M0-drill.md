# M0 多 Agent 协作演练

- 日期：2026-08-09
- 父任务：ECC-008
- 结果：本地开发和远程质量门禁通过，服务器部署链路阻塞

## 演练过程

ECC-009 与 ECC-010 从同一主线提交创建独立 Worktree，分别由低价模型实现 API 和 Web。两者共享预先冻结的 `/version` 响应结构，完成测试和 handoff 后按 API、Web 顺序合并。

## 指标

| 指标 | 结果 |
| --- | --- |
| 独立任务 | 2 |
| 首次验收通过 | 2/2 |
| 模型升级 | 0 |
| 合并冲突 | 0 |
| Python 测试 | 6/6 |
| Web 测试 | 4/4 |
| 远程质量门禁 | CI、Security、Docs Check 通过 |
| 镜像交付 | API/Web GHCR digest 镜像发布成功 |
| staging | SSH 服务器 Secrets 未配置，保持 blocked |

## 发现与调整

- 独立 Worktree 不共享主工作树的依赖目录，任务验收必须在各自工作树显式安装依赖。
- 仅有局部测试不足以发现容器入口、健康 URL 和浏览器跨域问题，因此保留合并后的统一验证阶段。
- 外部环境未配置时任务应 blocked，并写明可执行的解除条件；staging 使用显式启用变量，避免预期失败污染主线信号。
