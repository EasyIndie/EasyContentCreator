---
id: ECC-008
title: 并行协作与交付演练
status: blocked
milestone: M0
model: medium
depends_on: [ECC-004, ECC-005, ECC-006, ECC-007]
github_issue: null
branch: main
worktree: null
---

# ECC-008：并行协作与交付演练

## 目标与背景

通过两个互不冲突的低风险任务验证独立 Worktree、低价模型执行、冻结接口、顺序合并、handoff、质量门禁、staging 和回滚链路。

## 子任务与接口

- ECC-009：API `GET /version`，返回 `{ "version": string, "commit": string }`。
- ECC-010：Web 获取并显示同一结构，失败时显示 `unavailable`。
- 构建信息来自 `ECC_VERSION`、`ECC_COMMIT`，默认 `dev`、`unknown`。

## 验收标准

- 两项任务在独立 Worktree 完成并包含任务文件、测试和 handoff。
- 按 API 后 Web 顺序合并，统一验证全部通过。
- GitHub 必需检查实际通过，staging 版本一致，回滚演练成功。

## 验收命令

```bash
env PATH="$PWD/.venv/bin:$PATH" POSTGRES_PASSWORD=validation-only bash scripts/verify.sh
bash deploy/tests/test-deploy.sh
git diff --check
```

## 阻塞与解除条件

- 当前仓库没有 GitHub remote，无法创建 Draft PR、执行远程 Actions 或验证分支保护。
- Docker daemon 未运行，无法构建镜像、启动本地 Compose 或替代 staging 验收。
- 配置 GitHub remote、仓库 Secrets/Environments、单台 Linux staging，并启动可用 Docker 后，执行远程 CI、staging Smoke Test 和真实回滚即可解除。

## 执行记录

- 结果：ECC-009、ECC-010 已在独立 Worktree 完成并按顺序合并；零代码冲突。
- 测试：统一验证通过，Python 6 项、Web 4 项；部署参数守卫和健康探针测试通过。
- 偏差：本地提交代替 Draft PR/Squash 演练；未把本地验证视为远程 CI/CD 验收。
- 后续：满足外部条件后补齐 GitHub/staging/回滚证据，再完成 ECC-008 与 M0。
