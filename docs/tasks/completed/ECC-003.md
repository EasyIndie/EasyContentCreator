---
id: ECC-003
title: 架构基线与首批 ADR
status: completed
milestone: M0
model: high
depends_on: [ECC-002]
github_issue: null
branch: task/ECC-002-003-docs
worktree: /private/tmp/ecc-docs
---

# ECC-003：架构基线与首批 ADR

## 目标与背景

冻结 M0 工程脚手架、CI/CD 和后续流水线所需的架构边界。

## 文件范围

- 允许：`docs/architecture.md`、`docs/decisions/ADR-001..003*`、本任务和 handoff。
- 禁止：应用代码、数据库迁移、CI 和部署文件。

## 输入与接口

依赖 ECC-002；必须遵循 `docs/vision.md` 的规模和非目标。

## 实施要求

- 定义模块职责、状态、产物血缘、适配器协议、数据边界和部署策略。
- 分别接受模块化单体、不可变产物/适配器和 GitHub 持续交付 ADR。

## 验收标准

- ECC-004、005、007 无需重新选择架构即可实施。
- ADR 写明候选方案、代价和触发重新决策的条件。

## 验收命令

```bash
test -s docs/architecture.md
test "$(find docs/decisions -name 'ADR-*.md' | wc -l | tr -d ' ')" -eq 3
git diff --check
```

## 风险与回滚

公共边界变更必须新增 ADR 替代现有决策；本任务可整体回滚。

## 执行记录

- 结果：已完成架构正文与三项 accepted ADR。
- 测试：ADR 数量、链接一致性和 `git diff --check` 通过。
- 偏差：将 GitHub 持续交付单独成 ADR，便于 ECC-005/007 共同引用。
- 后续：实现发现边界不足时先提出新 ADR。
