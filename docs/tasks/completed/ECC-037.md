---
id: ECC-037
title: PipelineRun StepRun 持久化与版本预留
status: completed
milestone: M2
model: high
estimate: 2 days
depends_on: [ECC-036]
github_issue: null
branch: task/ECC-037-step-persistence
worktree: /private/tmp/ecc-037
---

# ECC-037：PipelineRun StepRun 持久化与版本预留

## 目标与范围

新增追加迁移与 Repository，保存 definition/run、StepRun、精确输入、logical key、reservation、Job 关联和失效关系。只改 `migrations`、pipeline/domain persistence、真实 PG 测试及文档；不得修改既有迁移。

## 验收

- fresh/upgrade PostgreSQL 16 保留 M1 数据；约束阻止重复活动 node/fingerprint 和重复 logical version。
- 同 key/hash 并发复用；异摘要冲突；同 logical key 单调预留，多镜头 key 可并存。
- 上游 ref 变化只失效依赖子图；事务注入失败全部回滚。
- Ruff、Mypy、真实双连接 pytest、全量相关测试、docs/diff 通过。

## 执行记录

- 结果：新增 003 additive migration、默认关闭且单向的 M2 kind 激活门禁、不可变 definition、PipelineRun/StepRun 精确 refs、多 logical streams/fanout reservations、Job enqueue、幂等/活动 fingerprint 与依赖失效 Repository。用户已明确批准激活/回滚边界。
- 测试：真实 PostgreSQL 16 覆盖 fresh/真实 002 upgrade、激活门禁、round-trip、双连接同 key、异摘要/活动冲突、fanout v2、事务回滚、NULL 输出唯一、SHA/ownership 绑定、终态 run 禁排、不可变数据和精确子图失效；完整证据见 handoff。
- 偏差：未实现 Step terminal、scheduler、Worker 或 API；这些属于 ECC-038 以后。
- 后续：ECC-038 在本 schema 上实现 live-lease terminal port 与 reconciler。
